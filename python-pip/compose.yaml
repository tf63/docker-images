services:
    python:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                - USER_UID=10001
                - USER_GID=10001
        container_name: 'python'
        tty: true
        volumes:
            - ./src:/app/src
